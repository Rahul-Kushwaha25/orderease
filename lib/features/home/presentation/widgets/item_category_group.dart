import 'package:flutter/material.dart';

class ItemCategoryGroup extends StatelessWidget {
  const ItemCategoryGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
